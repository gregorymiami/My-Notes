- Change the default branch name during repo creation
git config --global init.defaultBranch <name>

- Change the name of the current branch
git branch -m <name>

- Delete a local branch
git branch -d <branch>

- See who made the last change to each line in a file
git blame <file>